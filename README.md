The chatbot is initialized using ChatterBot.
It is trained using the English corpus dataset.
The Flask server handles user requests.
User input is sent to the backend via /get route.
The chatbot processes the input and returns a response.
The response is displayed on the web interfa
